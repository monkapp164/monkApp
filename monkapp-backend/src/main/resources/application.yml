spring:
  datasource:
    url: jdbc:mysql://localhost:3346/monkapp_db
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:monkapp164@gmail.com}
    password: ${MAIL_PASSWORD:mncahomsouhjemrr}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    secret: ${JWT_SECRET:v4VBvTVOPu6OUWEqzgZYPlxR9gZ+qFfzHT6vbaqbqeI=}
    expiration: 86400000
  verification:
    code-expiry-minutes: 10
  frontend-url: ${FRONTEND_URL:http://localhost:5173}

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    com.monkapp: INFO
    org.springframework.security: WARN
